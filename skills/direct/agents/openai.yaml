interface:
  display_name: "Direct"
  short_description: "Repeatable app states for browser agents, opened by URL"
  default_prompt: "Use $direct to add or verify a deterministic UI composition while keeping Direct out of production."
